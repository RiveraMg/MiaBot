# 📊 Diagrama de Base de Datos - MiaBot

## Modelo Entidad-Relación

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              DATOS MAESTROS                                  │
└─────────────────────────────────────────────────────────────────────────────┘

┌──────────────────┐
│    COMPANIES     │  ← Empresa principal (multi-tenant ready)
├──────────────────┤
│ id (PK)          │
│ name             │
│ nit (UNIQUE)     │
│ email            │
│ phone            │
│ address          │
│ logo             │
└────────┬─────────┘
         │
         │ 1:N (Una empresa tiene muchos...)
         │
    ┌────┴────┬──────────┬──────────┬──────────┬──────────┐
    ▼         ▼          ▼          ▼          ▼          ▼
┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐ ┌────────────┐
│ USERS  │ │PRODUCTS│ │CLIENTS │ │SUPPLIERS│ │INVOICES│ │CHAT_SESSIONS│
└────────┘ └────────┘ └────────┘ └─────────┘ └────────┘ └────────────┘


┌─────────────────────────────────────────────────────────────────────────────┐
│                           MÓDULO DE USUARIOS                                 │
└─────────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────┐
│         USERS            │
├──────────────────────────┤
│ id (PK)                  │
│ email (UNIQUE)           │
│ password (bcrypt)        │
│ name                     │
│ role (ENUM)              │──→ ADMIN | EMPLOYEE | EXTERNAL
│ department (ENUM)        │──→ FINANCE | HR (solo si EMPLOYEE)
│ phone                    │
│ avatar                   │
│ isActive                 │
│ position                 │
│ hireDate                 │
│ birthDate                │
│ contractEnd              │
│ companyId (FK)           │──→ companies.id
└──────────┬───────────────┘
           │
           │ 1:N
           ▼
┌──────────────────────────┐      ┌──────────────────────────┐
│    LEAVE_REQUESTS        │      │      NOTIFICATIONS       │
├──────────────────────────┤      ├──────────────────────────┤
│ id (PK)                  │      │ id (PK)                  │
│ type (ENUM)              │      │ type (ENUM)              │
│ status (ENUM)            │      │ title                    │
│ startDate                │      │ message                  │
│ endDate                  │      │ isRead                   │
│ reason                   │      │ referenceId              │
│ responseNote             │      │ referenceType            │
│ respondedAt              │      │ userId (FK)              │
│ userId (FK)              │      └──────────────────────────┘
└──────────────────────────┘


┌─────────────────────────────────────────────────────────────────────────────┐
│                        MÓDULO DE FINANZAS                                    │
└─────────────────────────────────────────────────────────────────────────────┘

┌──────────────────┐         ┌──────────────────┐
│   CATEGORIES     │ 1:N     │    PRODUCTS      │
├──────────────────┤────────→├──────────────────┤
│ id (PK)          │         │ id (PK)          │
│ name             │         │ sku (UNIQUE/co)  │
└──────────────────┘         │ name             │
                             │ description      │
                             │ stock            │◄── Alerta si stock ≤ minStock
                             │ minStock         │
                             │ unit             │
                             │ costPrice        │
                             │ salePrice        │
                             │ isActive         │
                             │ categoryId (FK)  │
                             │ companyId (FK)   │
                             └────────┬─────────┘
                                      │
                                      │ 1:N
                                      ▼
┌──────────────────┐         ┌──────────────────┐
│     CLIENTS      │ 1:N     │    INVOICES      │
├──────────────────┤────────→├──────────────────┤
│ id (PK)          │         │ id (PK)          │
│ name             │         │ number (UNIQUE)  │
│ email            │         │ status (ENUM)    │──→ DRAFT|SENT|PAID|OVERDUE|CANCELLED
│ phone            │         │ issueDate        │
│ address          │         │ dueDate          │◄── Alerta 3 días antes
│ nit              │         │ paidDate         │
│ companyId (FK)   │         │ subtotal         │
└──────────────────┘         │ tax (19% IVA)    │
                             │ total            │
                             │ notes            │
                             │ clientId (FK)    │
                             │ companyId (FK)   │
                             └────────┬─────────┘
                                      │
                          ┌───────────┴───────────┐
                          │ 1:N                   │ 1:N
                          ▼                       ▼
                 ┌──────────────────┐    ┌──────────────────┐
                 │  INVOICE_ITEMS   │    │     PAYMENTS     │
                 ├──────────────────┤    ├──────────────────┤
                 │ id (PK)          │    │ id (PK)          │
                 │ quantity         │    │ amount           │
                 │ unitPrice        │    │ method           │
                 │ total            │    │ reference        │
                 │ description      │    │ notes            │
                 │ productId (FK)   │    │ invoiceId (FK)   │
                 │ invoiceId (FK)   │    │ paidAt           │
                 └──────────────────┘    └──────────────────┘


┌──────────────────┐
│    SUPPLIERS     │
├──────────────────┤
│ id (PK)          │
│ name             │
│ email            │
│ phone            │
│ address          │
│ nit              │
│ contactName      │
│ companyId (FK)   │
└──────────────────┘


┌─────────────────────────────────────────────────────────────────────────────┐
│                          MÓDULO DE RRHH                                      │
└─────────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────┐
│         EVENTS           │
├──────────────────────────┤
│ id (PK)                  │
│ title                    │
│ description              │
│ type (ENUM)              │──→ MEETING|BIRTHDAY|ANNIVERSARY|EVALUATION|
│ startDate                │    CONTRACT_END|TRAINING|CORPORATE|DEADLINE|OTHER
│ endDate                  │
│ allDay                   │
│ reminderDays             │◄── Días antes para enviar recordatorio
│ isRecurring              │
│ companyId (FK)           │
└──────────────────────────┘


┌─────────────────────────────────────────────────────────────────────────────┐
│                           MÓDULO DE TAREAS                                   │
└─────────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────┐
│         TASKS            │
├──────────────────────────┤
│ id (PK)                  │
│ title                    │
│ description              │
│ status (ENUM)            │──→ PENDING|IN_PROGRESS|COMPLETED|CANCELLED
│ priority (ENUM)          │──→ LOW|MEDIUM|HIGH|URGENT
│ dueDate                  │
│ completedAt              │
│ department (ENUM)        │──→ FINANCE|HR (filtra por departamento)
│ createdById (FK)         │──→ users.id (quien creó)
│ assigneeId (FK)          │──→ users.id (asignado a)
└──────────────────────────┘


┌─────────────────────────────────────────────────────────────────────────────┐
│                           MÓDULO DE CHAT                                     │
└─────────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────┐         ┌──────────────────────────┐
│     CHAT_SESSIONS        │ 1:N     │     CHAT_MESSAGES        │
├──────────────────────────┤────────→├──────────────────────────┤
│ id (PK)                  │         │ id (PK)                  │
│ type (ENUM)              │──→      │ content                  │
│   INTERNAL (empleados)   │         │ role (ENUM)              │──→ USER|ASSISTANT|SYSTEM
│   EXTERNAL (público)     │         │ metadata (JSON)          │
│ visitorName              │         │ sessionId (FK)           │
│ visitorEmail             │         │ userId (FK)              │◄── NULL si es externo
│ visitorPhone             │         │ createdAt                │
│ context                  │         └──────────────────────────┘
│ isActive                 │
│ companyId (FK)           │
└──────────────────────────┘


┌─────────────────────────────────────────────────────────────────────────────┐
│                           MÓDULO DE ARCHIVOS                                 │
└─────────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────┐
│         FILES            │
├──────────────────────────┤
│ id (PK)                  │
│ name                     │
│ mimeType                 │
│ size                     │
│ storageType              │──→ "local" | "google_drive"
│ path                     │◄── Para archivos locales
│ driveFileId              │◄── Para Google Drive
│ driveUrl                 │
│ referenceId              │◄── ID del objeto relacionado
│ referenceType            │◄── "invoice", "contract", etc.
│ uploadedById             │
└──────────────────────────┘
```

## 📋 Resumen de Tablas

| # | Tabla | Descripción | Módulo |
|---|-------|-------------|--------|
| 1 | companies | Empresas (dato maestro) | Core |
| 2 | users | Usuarios del sistema | Core |
| 3 | categories | Categorías de productos | Finanzas |
| 4 | products | Inventario | Finanzas |
| 5 | clients | Clientes | Finanzas |
| 6 | suppliers | Proveedores | Finanzas |
| 7 | invoices | Facturas | Finanzas |
| 8 | invoice_items | Líneas de factura | Finanzas |
| 9 | payments | Pagos recibidos | Finanzas |
| 10 | events | Eventos y reuniones | RRHH |
| 11 | leave_requests | Solicitudes de permisos | RRHH |
| 12 | tasks | Tareas | Compartido |
| 13 | chat_sessions | Sesiones de chat | Chat |
| 14 | chat_messages | Mensajes de chat | Chat |
| 15 | notifications | Notificaciones | Core |
| 16 | files | Archivos | Core |

## 🔐 Acceso por Rol

```
ADMIN
  └── Acceso TOTAL a todas las tablas

EMPLOYEE (FINANCE)
  ├── products ✅
  ├── categories ✅
  ├── clients ✅
  ├── suppliers ✅
  ├── invoices ✅
  ├── invoice_items ✅
  ├── payments ✅
  ├── tasks (solo FINANCE) ✅
  └── chat_sessions (INTERNAL) ✅

EMPLOYEE (HR)
  ├── users (solo empleados) ✅
  ├── events ✅
  ├── leave_requests ✅
  ├── tasks (solo HR) ✅
  └── chat_sessions (INTERNAL) ✅

EXTERNAL
  └── chat_sessions (EXTERNAL) ✅ (solo lectura pública)
```

## 🔔 Alertas Automáticas (para n8n)

1. **Stock Bajo**: `products.stock <= products.minStock`
2. **Factura por Vencer**: `invoices.dueDate` - 3 días
3. **Factura Vencida**: `invoices.dueDate < HOY` y `status = 'SENT'`
4. **Contrato por Vencer**: `users.contractEnd` - 30 días
5. **Cumpleaños**: `users.birthDate` próximos 7 días
6. **Permiso Pendiente**: `leave_requests.status = 'PENDING'`
